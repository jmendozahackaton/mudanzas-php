{
    "name": "mudanzas-app/api",
    "description": "API monolítica para plataforma de mudanzas",
    "type": "project",
    "require": {
        "php": "^8.4",
        "ext-pdo": "*",
        "ext-json": "*",
        "ext-mbstring": "*",
        "ext-gd": "*"
    },
    "require-dev": {
        "google/cloud-tools": "^0.10.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        },
        "files": [
            "src/config/database.php"
        ]
    },
    "scripts": {
        "start": "php -S localhost:8080 -t src",
        "deploy": "gcloud run deploy api-mudanzas --source . --region us-central1 --allow-unauthenticated"
    },
    "config": {
        "platform": {
            "php": "8.4"
        },
        "optimize-autoloader": true
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
